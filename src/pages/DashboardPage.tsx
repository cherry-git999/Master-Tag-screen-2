import { Layout } from "@/components/layout/Layout";

export function DashboardPage() {
  return (
    <Layout>
      <div className="p-6">
        <div className="mb-1 text-xs text-gray-500">Dashboard</div>
        <h1 className="text-xl font-semibold">Dashboard</h1>
      </div>
    </Layout>
  );
}